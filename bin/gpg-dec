#!/bin/bash

if [[ ! $# -eq 1 ]]; then
	echo "usage: gpg-dec <file>"
	exit 1
fi
file="$1"
new_file="${file%.gpg}"

gpg --output "$new_file" --decrypt "$file"
echo "Decrypted '$file' into '$new_file'"
